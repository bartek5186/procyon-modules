# Example module

Minimal reference compile-time plugin for Procyon. It owns its route and
exposes `GET /v1/example` through the plugin contract.

It exists to test the module format; production projects normally install a
domain module such as `payment-system`.
